import { StyleSheet } from "react-native";

export const gridItemStyles = StyleSheet.create({
    itemContainer: {
        justifyContent: 'center',
        alignItems: 'center',
        width: '46%',
        aspectRatio: 1,
        margin: '2%',
        borderWidth: 2,
        borderColor: 'black',
        borderRadius: 10
    }
});